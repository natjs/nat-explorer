{
    "prepare_queue": {
        "installed": [],
        "uninstalled": []
    },
    "config_munge": {
        "files": {}
    },
    "installed_plugins": {
        "nat-camera": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-communication": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-device-battery": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-device-info": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-device-network": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-device-screen": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-device-vibration": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-device-volume": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-geolocation": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-media-audio": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-media-image": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-media-video": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-modal": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-recorder": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-sensor-accelerometer": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-sensor-compass": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-stream": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        },
        "nat-transfer": {
            "PACKAGE_NAME": "$(PRODUCT_BUNDLE_IDENTIFIER)"
        }
    },
    "dependent_plugins": {}
}